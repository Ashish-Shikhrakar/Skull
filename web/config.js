// Your Supabase project's API settings (Project Settings → API).
// The anon key is meant to be public — every write goes through the RPCs in sql/schema.sql.
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''
